#pragma once

#include <vector>
#include <string>
#include <assert.h>

namespace aiko::recorder
{

    class LoopPlayer
    {
    public:

        void init();

        void playAudioFile(const std::string audioFile);

    private:

    };

}

